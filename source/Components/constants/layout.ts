enum layoutTypes {
    HORIZONTAL = 'horizontal',
    VERTICAL = 'vertical',
    TWO_COLUMN = 'two_column',
}

enum layoutModeTypes {
    LIGHT_MODE = 'light',
    DARK_MODE = 'dark',
}

enum leftSidebarTypes {
    LIGHT = 'light',
    DARK = 'dark',
    GRADIENT = 'gradient',
    GRADIENT_2 = 'gradient-2',
    GRADIENT_3 = 'gradient-3',
    GRADIENT_4 = 'gradient-4',
}

enum layoutWidthTypes {
    FLUID = 'lg',
    BOXED = 'boxed',
}

enum layoutPositionTypes {
    FIXED = 'fixed',
    SCROLLABLE = 'scrollable',
}

enum topBarThemeTypes {
    LIGHT = 'light',
    DARK = 'dark',
}

enum leftSideBarSizeTypes {
    DEFAULT = 'lg',
    COMPACT = 'md',
    SMALL_ICON = 'sm',
    SMALL_HOVER = 'sm-hover',
}

enum leftSidebarViewTypes {
    DEFAULT = 'default',
    DETACHED = 'detached',
}

enum leftSidebarImageTypes {
    NONE = 'none',
    IMG1 = 'img-1',
    IMG2 = 'img-2',
    IMG3 = 'img-3',
    IMG4 = 'img-4',
}

enum preloaderTypes {
    ENABLE = 'enable',
    DISABLE = 'disable',
}

export {
    layoutModeTypes,
    layoutPositionTypes,
    layoutTypes,
    layoutWidthTypes,
    leftSideBarSizeTypes,
    leftSidebarImageTypes,
    leftSidebarTypes,
    leftSidebarViewTypes,
    preloaderTypes,
    topBarThemeTypes,
};
